import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.uao.mapsapp',
  appName: 'Maps Ionic',
  webDir: 'dist',
  server: { androidScheme: 'https' },
};

export default config;
